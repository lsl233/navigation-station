---
import LangPage from "@/components/langPage/index.astro";
import { LOCALES } from "@/i18n";

export const getStaticPaths = () =>
  Object.keys(LOCALES).map((lang) => ({
    params: { lang },
  }));

const lang = Astro.params.lang;

---

<LangPage lang={lang}></LangPage>
